# Environment / secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# uv
# uv.lock is committed so Docker builds can use --frozen

# fuin runtime artifacts
data/

# Android / Gradle
stub/.gradle/
stub/build/
stub/app/build/
stub/local.properties
stub/gradle.properties
*.keystore
*.jks

# Large binary deps for local Docker builds on corporate networks (never commit)
.docker-deps/

# macOS
.DS_Store
