.git/
.venv/
.env
.ruff_cache/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Runtime artifacts (mounted as volume in container)
packed_apks/
fuin.db

# JVM sources and build artifacts — not needed by the server image, which
# uses the stub.dex shipped inside the Python package.
jvm/

*.keystore
*.jks
